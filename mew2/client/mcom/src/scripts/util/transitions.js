/**
 * @file transitions.js
 * Currently a dummy file since bcom uses in common global events view
 *
 * @TODO - bcom should decouple transitions from common global events view
 */

define([
], function() {

  var Transitions = function() {};

  return Transitions;
});
