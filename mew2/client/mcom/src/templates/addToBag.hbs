
<div class="m-header boldtext">
  <button class="mb-j-overlay-close m-modal-button">&nbsp;</button>
  <span>{{itemQuantity}} item{{#if_gt itemQuantity compare="1" }}s{{/if_gt}} added to {{userName}} bag</span>
</div>
<div class="m-content">
  <div class="m-image-gr">
    <img src="{{#displayConfig}}{{paths.product.image}}{{/displayConfig}}{{image.image}}" class="m-upc-img" />
    {{#if image.swatch}}
      <img src="{{#displayConfig}}{{paths.product.image}}{{/displayConfig}}{{image.swatch}}" class="m-upc-swatch" />
    {{/if}}
  </div>

  <div class="m-upc-details">
    <h6>{{product.name}}</h6>

    {{> addToDetails }}

    <div class="m-product-price-gr">
      {{#if product.isEGC}}
        <div class="m-product-price">{{egcPrice.amt}}</div>
      {{else}}
        {{#each product.prices}}
          <div class="m-product-price {{stripNonAlpha label}}">
            {{#if label}}
              <span class="m-product-price-label">{{label}} </span>
            {{/if}}
            <span class="m-product-price-amt">{{amt}}</span>
          </div>
        {{/each}}
      {{/if}}
    </div>

    {{#if youSaved}}
      <p class="m-you-saved">You just saved ${{youSaved}}!</p>
    {{/if}}
  </div>
</div>
<div class="m-footer">
  <hr />
  <p>{{bag.totalquantity}} item{{#if_gt bag.totalquantity compare="1"}}s{{/if_gt}} in <a class="m-go-bag" href="/bag/index.ognc">{{userName}} bag</a></p>
  <p class="boldtext">subtotal: ${{bagTotal}}</p>

  <div class="m-bag-buttons">
    <button class="mb-j-overlay-continue">continue shopping</button>
    <a class="m-checkout" href="/bag/index.ognc">checkout</a>
  </div>
</div>